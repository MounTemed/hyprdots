{ ... }:
{
  wayland.windowManager.hyprland.settings = {
    xwayland = {
      enabled = false;
      # force_zero_scaling = true;
    };

    # render = {
    #   new_render_scheduling = true;
    # };
  };
}
